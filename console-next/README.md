# ohmyqwen Next.js Console

런타임 API(`ohmyqwen serve`)에 붙는 Next.js 기반 대시보드 UI.

## 실행

```bash
pnpm install
pnpm dev
```

기본 URL: `http://127.0.0.1:3000`

## 백엔드 API 주소

기본값: `http://127.0.0.1:4311`

변경하려면 `.env.local` 생성:

```env
BACKEND_BASE_URL=http://127.0.0.1:4311
```
